import { Typography, Box } from '@material-ui/core/'

export const Home = () => {
    return (
        <Box>
            <Typography variant='h1' color='initial'>
                Home page
            </Typography>
            <Typography variant='h5' color='initial'>
                Elija una categoría desde la carta digital.
            </Typography>
        </Box>
    )
}
